name: PR Title Check

on:
  pull_request:
    # synchronize 필수: 필수 상태 체크는 head 커밋 기준이라, 빠지면 push 후 validate-title 체크런이 없어 머지가 영구 블록된다 (#625)
    types: [opened, edited, reopened, synchronize]

jobs:
  validate-title:
    runs-on: ubuntu-latest

    steps:
      - name: Validate PR title format
        shell: bash
        # 제목은 env 로만 주입한다 — run 에 ${{ }} 직접 보간하면 fork PR 제목으로 셸 명령이 실행될 수 있다 (#628)
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          echo "PR Title: $TITLE"

          # 다중 이슈 번호를 허용하는 정규표현식 (예: #1, 2, 4 또는 #1, #2, #3)
          REGEX='^\[(Feat|Fix|Refactor|Docs|Test|Chore|Infra)\] #[0-9]+([, ]+#?[0-9]+)* .+$'

          if [[ ! "$TITLE" =~ $REGEX ]]; then
            echo "PR 제목 형식이 올바르지 않습니다."
            echo "허용 형식(단일): [Feat] #23 로그인 API 구현"
            echo "허용 형식(다중): [Chore] #1, 2, 4 설정 파일 업데이트"
            echo "허용 TYPE: Feat, Fix, Refactor, Docs, Test, Chore, Infra"
            exit 1
          fi

          echo "PR 제목 형식이 올바릅니다."