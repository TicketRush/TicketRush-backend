SERIES_ID=260731-546-queue-status
ISSUE=#546 (귀속: #472, 배포 묶음 #512 D)
PR=#548
DATE=2026-07-31 (KST)

# ── 토폴로지 ────────────────────────────────────────────────────────────────
TARGET_HOST=EC2 54.116.243.250 — https://api.ticketrush.store
TOPOLOGY=k6 local (docker, ADR 0004) --SSH tunnel(9090)--> EC2 deploy stack (ADR 0007)
# ⚠ ADR 0010(AWS 내부 생성기)은 회차 B(1만 VU) 전용이다. 이 회차(A)는 계단 상한이 1600 RPS 라
#   ADR 0004 토폴로지(k6=로컬)로 돌렸다. B 와 절대값을 직접 잇지 않는다.
INSTANCE_TYPE=m7i-flex.large / INSTANCE_VCPU=2
RUNBOOK=docs/load-test-guide.md §16
SCENARIO=load-test/scenarios/waiting-room.js (QUEUE_PROFILE=status)
SEED=없음 — 대기열은 Redis 만 쓴다. setup() 이 QUEUE_PRELOAD_SIZE 명을 진입시킨다
IMAGE_TAG=21d2da2d6fc2e3bfdaf27f2b1440beccaad337bf
PROM_SCRAPE_INTERVAL=15s
CLIENT_RTT=약 45ms (health 4회 평균, 로컬→EC2)

# ── 사전 게이트 (런북 §16.4 G0~G7) ─────────────────────────────────────────
G0_EC2=기동 / IMAGE_TAG=21d2da2d (PR #545 배포, CD 20분 07초)
G1_NGINX=조치함 — worker_connections 768 → 16384, worker_rlimit_nofile 신설 65535
#   변경 전 실측: worker_connections 768(Ubuntu 기본값), worker_processes auto → nproc 2,
#   워커 fd soft 1024. location / 에 upstream 블록이 없어 keepalive 지시자가 구조적으로 부재라
#   프록시 요청 1건이 슬롯 2개(클라이언트+업스트림)를 먹는다 → 실질 동시 요청 약 768.
#   회차 A 는 maxVUs 800 → 1,600 슬롯이 필요해 변경 전 상태로는 앱이 아니라 nginx 를 쟀을 것이다.
#   변경 후: 16384 × 2워커 = 32,768 슬롯, 워커 2개 모두 fd 65535 확인. reload 무중단(health 200/47ms).
#   백업 /etc/nginx/nginx.conf.bak.20260731. 호스트에만 있는 설정이라 #522 에 코멘트로 기록.
G2_QUEUE_ENABLED=true (deploy/.env, 게이트웨이 --force-recreate 로 반영)
G2_QUEUE_ADMIT_RATE=1
#   기본값 20 이면 threshold = 경과 × 20 이 PRELOAD 1만을 8분 20초에 넘어서고, 회차는 20분이라
#   폴링 대상이 도중에 승급한다. 승급 후 폴링은 입장 토큰 SET 이 붙어 Redis 명령이 2회→3회가 되어
#   '대기 중인 사용자의 폴링' 이 아닌 다른 경로를 재게 된다(R 과소평가). 로컬 스모크 확증:
#   rate 20 → queue_status_admitted_leak 77.48%, rate 1 → 0.00%.
G3_METRICS=ticketrush_queue_* 9개 시계열 전부 0 으로 노출 확인(사전 등록 패턴)
G4_REDIS=used_memory_human 1.71M / maxmemory 64.00M / policy noeviction
G5_PROM_TARGETS=up (CD 게이트가 UP_COUNT==11 && DOWN_COUNT==0 강제)
G6_GENERATOR=로컬 k6 (위 TOPOLOGY 참조)
G7_QUEUE_RESET=queue:* 0 개 확인 후 시작
#   ⚠ 스모크가 남긴 1,002 키(waiting-token 500 + user-token 500 + ZSET 1 + opened-at 1)를
#   본 회차 전에 삭제했다. 컨테이너명이 ticketrush-redis 라 'redis' 로 조회하면 조용히 0 이 나온다.

# ── 회차 전 처치 ───────────────────────────────────────────────────────────
GATEWAY_RECREATE=2026-07-31T13:0x:xxZ (env_file 반영 위해 restart 가 아니라 --force-recreate)
SMOKE=PRELOAD 500 / 계단 100,200 × 30s → 실패 0, unavailable 0%, admitted_leak 0%,
#      p95 24.05ms, avg 10.79ms, 138.7 RPS. 수치는 폐기(§6.1 규칙 4), 계단 하한 근거로만 쓴다.

# ── 계단 확정 근거 ─────────────────────────────────────────────────────────
RAMP_STAGES=200,400,800,1600 × 5m (계단 사이 램프 10s)
#   200 부터 시작하는 근거: 이 경로는 GET(대기 토큰) + ZRANK 2회이고 DB 를 타지 않는다
#   (PR #544 통합 테스트가 INFO commandstats 로 고정). 게이트웨이 홉 + DB 집계를 포함한
#   #529 seat-counts 포화점 396.75 RPS 보다 빠를 수밖에 없다.
QUEUE_PRELOAD_SIZE=10000
#   ZRANK 는 skiplist 탐색이라 ZSET 크기에 로그 비례한다. 빈 대기열을 재면 R 을 과대평가하고
#   그 값이 그대로 폴링 주기 하한이 되어 운영에서 터진다. 폴링 대상은 마지막 진입자(순번 최후미)라
#   탐색 비용이 가장 큰 지점 = 회차가 보수적인 방향으로 틀린다.
PRE_ALLOCATED_VUS=100 / MAX_VUS=800

# ── 판정 기준 (회차 전에 정한다 — §6.1) ────────────────────────────────────
KNEE_CRITERIA=도착률을 올려도 실제 RPS 가 따라오지 않는 첫 계단 + dropped_iterations 발생
#              + 호스트 CPU 수렴. 세 신호가 갈리면 '실제 RPS 정체' 를 우선한다.
AB_CRITERIA=keepalive_timeout 75s(A-1) vs 15s(A-2) 를 아래 순서로 가른다.
#   1순위: 호스트 CPU (peak·계단별 평균) — CPU 가 이 구성의 확정된 벽이다(#509·#518)
#   2순위: 게이트웨이·nginx 메모리 (mem_limit 512m 대비, 호스트 여유)
#   3순위: k6_http_req_connecting_p95 · k6_http_req_tls_handshaking_p95 (재핸드셰이크 비용)
#   1순위 차이가 5%p 이내면 2순위로, 그것도 10% 이내면 3순위로 내려간다.
#   ⚠ 이 기준은 회차 전에 확정했다. 결과를 보고 고치지 않는다.
INVALID_IF=queue_status_unavailable > 0 (fail-closed 발동 = Redis 장애를 잰 것)
INVALID_IF=queue_status_admitted_leak > 0 (폴링 대상 승급 = 측정 경로가 2회→3회로 바뀜)
INVALID_IF=redis_memory_used_bytes ≥ 48MB (noeviction 상한 근접 — 좌석 락 SET 거절 위험)

# ── 실측 (회차 후 채운다) ──────────────────────────────────────────────────
A1_KEEPALIVE=75s (nginx 설정에 keepalive_timeout 지시자가 없어 컴파일 기본값)
A1_START_UTC=2026-07-31T13:08:52Z
A1_END_UTC=
A1_RESULT=
A2_KEEPALIVE=15s
A2_START_UTC=
A2_END_UTC=
A2_RESULT=
KNEE_R=
POLL_INTERVAL_T=
VERDICT=
WINDOW_END_SOURCE=
