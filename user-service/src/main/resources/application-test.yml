app:
  event-publisher:
    type: kafka

# base 는 기본값 없는 ${INTERNAL_API_TOKEN}·${GATEWAY_INTERNAL_TOKEN} 을 요구한다(운영에서 미주입
# 기동을 막기 위함). 컨텍스트 로딩 테스트가 뜨도록 더미값만 채운다 — 다른 서비스 base 의
# 기본값과 같은 문자열이다(#500).
custom:
  security:
    internal-token: test-internal-token

gateway:
  internal-token: test-token
